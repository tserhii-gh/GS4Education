using System;

namespace Circle
{
    class Program
    {
        static void Main(string[] args)
        {
            double radius;
            double area;

            radius = 10.0;
            area = radius * radius * 3.146;
            Console.WriteLine("Circle area is => " + area);

        }
    }
}
